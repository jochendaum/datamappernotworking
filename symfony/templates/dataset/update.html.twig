{% extends 'base.html.twig' %}

{% form_theme form with ['bootstrap_3_horizontal_layout.html.twig','form/fields.html.twig'] %}

{% block admin -%}
    <div class="row">
        <div class="col-sm-6">
            <h1>Create data sets</h1>
        </div>
        <div class="col-sm-3">
        </div>
        <div class="col-sm-3 paging">
        </div>
    </div>

    {% include 'dataset/_form.html.twig' %}

    <div class="row">
        <div class="col-sm-6">

        </div>
        <div class="col-sm-3">
        </div>
        <div class="col-sm-3">
            {{ form_start(delete_form) }}
            <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </div>
    </div>

{% endblock %}
